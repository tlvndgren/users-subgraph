directive @tag(name: String!) repeatable on
  | FIELD_DEFINITION
  | INTERFACE
  | OBJECT
  | UNION

type User @key(fields: "id") {
    id: ID!
    firstName: String
    lastName: String
    userScore: Int @tag(name: "private")
}

extend type Query {
    users: [User] 
    user(id: ID!): User!
}